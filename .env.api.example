# ========================================
# iapi API 节点配置示例
# ========================================
# 用途：只提供 HTTP 接口、提交任务、查询任务状态；不加载 GPU 模型，不执行图片生成。
# 启动：uvicorn app.main:app --host 0.0.0.0 --port 8000

HOST=0.0.0.0
PORT=8000

# 返回给 OpenAI/New API 兼容接口的模型名；API 节点不会因此加载模型。
MODEL_NAME=qwen-image-2512
QWEN_IMAGE_MODEL_NAME=qwen-image-2512

# API/Worker 拆分时必须使用 api + redis。
SERVICE_ROLE=api
TASK_QUEUE_BACKEND=redis

# API 和所有 Worker 必须完全一致。
REDIS_URL=redis://127.0.0.1:6379/0
REDIS_QUEUE_NAME=iapi:image_tasks
REDIS_PROCESSING_QUEUE_NAME=iapi:image_tasks:processing
REDIS_BLOCK_TIMEOUT=5
REDIS_SOCKET_CONNECT_TIMEOUT=10
REDIS_SOCKET_TIMEOUT=30
IMAGE_QUEUE_MAXSIZE=100

# API 和所有 Worker 必须连接同一个任务数据库；生产拆分部署推荐 MySQL。
TASK_DB_BACKEND=mysql
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=iapi
MYSQL_PASSWORD=replace-with-your-mysql-password
MYSQL_DATABASE=iapi
MYSQL_CHARSET=utf8mb4
MYSQL_CONNECT_TIMEOUT=10

# 任务查询返回用的 API 公网地址；例如 https://api.example.com
TASK_PUBLIC_BASE_URL=

# API 节点如果不和 Worker 共享本地 outputs 目录，生产建议配置 OSS。
# 配置 OSS 后，Worker 生成的图片会返回 OSS URL，API 不依赖本地文件。
OUTPUT_DIR=outputs
PUBLIC_BASE_URL=
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
OSS_ACCESS_KEY_ID=your_access_key_id
OSS_ACCESS_KEY_SECRET=your_access_key_secret
OSS_BUCKET=your_bucket_name
OSS_PUBLIC_BASE_URL=
OSS_OBJECT_PREFIX=images
OSS_RETENTION_DAYS=14

# STS 如不使用可留空。
OSS_STS_ROLE_ARN=
OSS_STS_DURATION=3600
ALIYUN_REGION_ID=cn-hangzhou

# API 节点通常不需要 Hugging Face / CUDA 配置。
HF_TOKEN=
TOKENIZERS_PARALLELISM=false